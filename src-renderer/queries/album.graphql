query album($id: Int!) {
    album(id: $id) {
        id
        title
        genre
        year
        leadArtists {
            id
            name
        }
        musics {
            ...PlayableMusic
            duration
            year
        }
    }
}
