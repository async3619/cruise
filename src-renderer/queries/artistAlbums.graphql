query artistAlbums($artistId: Int!) {
    artist(id: $artistId) {
        id
        name
        musicCount
        albums {
            id
            title
            year
            leadArtists {
                id
                name
            }
            musics {
                ...PlayableMusic
                genre
                duration
            }
        }
    }
}
