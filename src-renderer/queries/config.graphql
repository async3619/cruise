query config {
    config {
        libraryDirectories
        appTheme
        volume
        muted
        repeatMode
    }
}

mutation updateConfig($config: ConfigInput!) {
    updateConfig(config: $config) {
        libraryDirectories
        appTheme
        volume
        muted
        repeatMode
    }
}
