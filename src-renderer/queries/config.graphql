query config {
    config {
        libraryDirectories
        appTheme
        volume
        muted
    }
}

mutation updateConfig($config: ConfigInput!) {
    updateConfig(config: $config) {
        libraryDirectories
        appTheme
        volume
        muted
    }
}
