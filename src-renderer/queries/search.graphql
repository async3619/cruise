query search($query: String!) {
    search(query: $query) {
        total
        musics {
            ...PlayableMusic
        }
        albums {
            ...AlbumListItem
        }
        artists {
            id
            name
        }
    }
}
