query albums {
    albums {
        id
        title
        leadArtists {
            id
            name
        }
        musics(limit: 1) {
            id
            albumArts {
                id
                path
            }
        }
    }
}
