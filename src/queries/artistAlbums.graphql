query artistAlbums($artistId: Int!) {
    artist(id: $artistId) {
        id
        name
        albums {
            id
            title
            year
            leadArtists {
                id
                name
            }
            musics {
                ...PlayableMusic
                genre
                duration
            }
        }
    }
}
