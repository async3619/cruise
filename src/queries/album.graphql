query album($id: Int!) {
    album(id: $id) {
        id
        title
        leadArtists {
            id
            name
        }
        musics {
            ...PlayableMusic
            duration
            year
        }
    }
}
